[日本語](#技術文書)｜[English](#technical-document)｜[`.bash_profile`](../src/.bash_profile)

# 技術文書

`.bash_profile`ファイルの設定は、ログイン時に１度だけ読み込まれる。

## 目次

- [設定を参照する](#設定を参照する)

## 設定を参照する

```bash
if
  [[ -f ${HOME}/.bashrc ]]
then
  source ${HOME}/.bashrc
fi
```

# Technical Document

Configuration in the `.bash_profile` file loads once when you log in.

## Table of Contents

- [Reference Configuration](#reference-configuration)

## Reference Configuration

```bash
if
  [[ -f ${HOME}/.bashrc ]]
then
  source ${HOME}/.bashrc
fi
```