[日本語](#技術文書)｜[English](#technical-document)｜[`.bash_profile`](../src/.bash_profile)

# 技術文書

```bash
if
  [[ -f ${HOME}/.bashrc ]]
then
  source ${HOME}/.bashrc
fi
```

# Technical Document

```bash
if
  [[ -f ${HOME}/.bashrc ]]
then
  source ${HOME}/.bashrc
fi
```